.DS_Store
.next/
.sites-runtime/
.wrangler/
dist/
node_modules/
node_modules */
*.tsbuildinfo
*.log

.env
.env.*
!.env.example

# YouTube / Project BULLSEYE video automation (operator OAuth — never commit)
tools/youtube-upload/.env.local
tools/youtube-upload/.tokens/
tools/youtube-upload/client_secret*.json
**/client_secret*.json
**/youtube-oauth.json

# Local agent / preview scratch (never commit)
.tmp-screens/
.vinext/
tools/youtube-upload/.tmp/
next-env.d.ts

# Playwright / Project Bullseye audit artifacts (never commit secrets or storage state)
audit-output/
audit-output/**
audit-output-run-*.log
project-bullseye-audit.zip
playwright-report/
test-results/
blob-report/
.auth/
**/storage-state.json
playwright/.auth/
visual-review/
audit/.env
audit/.env.local

# Android release signing material (never commit)
*.jks
*.keystore
**/key.properties
**/keystore.properties

# iCloud conflict / placeholder stubs
*.icloud
**/* 2.*
**/* 3.*
