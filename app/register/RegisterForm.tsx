"use client";import{FormEvent,useState}from"react";
export default function RegisterForm(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState(""),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setMessage("");try{const r=await fetch("/api/register",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok)setMessage(d.error);else window.location.assign("/login")}catch{setMessage("Account setup is temporarily unavailable.")}finally{setLoading(false)}}return <form className="loginForm" onSubmit={submit}><label htmlFor="email">STRIPE CHECKOUT EMAIL</label><input id="email" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><label htmlFor="password">CREATE PASSWORD</label><input id="password" type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/><button className="primary" disabled={loading}>{loading?"Creating account…":"Create member account"}<span>↗</span></button>{message&&<p className="loginMessage">{message}</p>}</form>}
