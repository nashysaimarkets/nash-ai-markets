import type{Metadata}from"next";import RegisterForm from"./RegisterForm";export const metadata:Metadata={title:"Create Member Account"};export default function Register(){return <main className="outcome loginPage"><div className="outcomeCard loginCard"><span className="outcomeMark">✓</span><p className="kicker">PAID MEMBER SETUP</p><h1>Create your<br/><em>Terminal access.</em></h1><p>Use the same email address used for your Stripe subscription. Only verified active memberships can create an account.</p><RegisterForm/><small>Already registered? <a href="/login">Member login</a> · <a href="mailto:hello@nashaimarkets.com">Get help</a></small></div></main>}
