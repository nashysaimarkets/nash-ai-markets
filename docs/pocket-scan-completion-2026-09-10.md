# Scan completion and preflight cancellation — 10 September 2026

The 15:58 UTC five-image request on Vercel deployment `dpl_3c5FUhyXMJ4C3PidptskaXZ4SHZE` failed after 150.7 seconds. Both precision passes completed by 24.5 seconds. The original report began output at 36.5 seconds but ended at 62.5 seconds with 9,958 characters; recovery started at 60.7 seconds, began output at 125.1 seconds and ended at its fixed cutoff with 9,514 characters. No AI quota failure was logged. The preflight requests logged HTTP 200, while the supplied phone screenshot showed its client timeout, so the exact client/network timing is not established.

The overlapping report scheduler previously shortened the original attempt's 75-second window to a 15-second idle deadline after output began. Recovery already starts independently at 60 seconds, so discarding the original early provides no scheduling benefit. Overlapping attempts now preserve their initial window. Either active attempt may use the existing 30-second progress extension, always capped by the existing overall report deadline (165 seconds for the full-parallel five-image profile). Recovery previously ignored output progress entirely. The two-attempt maximum, full image pack, model, reasoning, schema and precision gates remain unchanged.

Preflight now propagates upload cancellation and enforces a 25-second whole-response deadline. The installed SDK's transport timer alone ends when HTTP headers arrive; it does not bound a stalled body. Superseded checks therefore previously continued provider work. Preflight uses priority scheduling and a 2,400-token ceiling to allow both low-effort reasoning and the five-image structured check; no new model or retry is added. Failed preflight allowance is released. Completion and cancellation timing are logged without chart contents. Report cancellation logs now include output-idle time and cancellation cause.

Controlled regressions reproduce a report pause at 47 seconds followed by completion at 70 seconds, and a recovery starting output at 125 seconds and completing at 157 seconds. They also enforce the unchanged 165-second deadline, cancellation, quota handling, strict complete-response validation and two-attempt limit. Preflight tests execute the actual route with the installed OpenAI SDK and a body that stalls after headers, cancellation on image replacement, and successful multi-image mismatch detection. These establish timer behavior, not guaranteed live-provider latency or chart accuracy.
